---
import HeroBoxed from '@components/macro/hero/HeroBoxed.svelte'
import Layout from '@layouts/Base.astro'
const { seo, hero } = Astro.props.frontmatter
---

<Layout {seo}>
  <!-- <HeroBoxed content={hero} /> -->
  <!-- <Carousel /> -->
</Layout>
