---
import CardCollection from '@components/meso/card/CardCollection.svelte'
import CardProduct from '@components/meso/card/Product.svelte'
import Carousel from '@components/meso/Carousel.svelte'
import Layout from '@layouts/Base.astro'
const { seo } = Astro.props.frontmatter

import { getCollections, getProducts } from 'public/shopify'

let collections = await getCollections()
let products = await getProducts()
console.log(products[0])
---

<Layout {seo}>
  <Carousel
    items={products}
    client:load
  />
  <Carousel
    items={collections}
    client:load
  />
</Layout>
